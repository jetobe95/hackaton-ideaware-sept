const InitialState = {
  Queue: {},
  larger: false,
  random: false,
  mute: false
};

const PlayerReducer = (state = InitialState, action) => {
  switch (action.type) {
    case '':
      break;
    default:
      return state;
  }
};
export default PlayerReducer;
